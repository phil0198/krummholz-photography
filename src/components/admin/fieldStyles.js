// Shared Tailwind classes for the plain form controls in /admin, so the
// dashboard doesn't repeat the same long class string on every input.
export const fieldClass =
  "border border-ink-3 bg-ink-2 px-3 py-2 text-sm text-bone outline-none focus-visible:border-accent";
export const fieldLabelClass = "text-xs uppercase tracking-[0.15em] text-smoke";
